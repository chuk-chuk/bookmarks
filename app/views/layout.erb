<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link href="<%= url("/css/custom.css")%>" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Quicksand:300|Fredoka+One" rel="stylesheet">
    <title>Bookmarks</title>
  </head>

  <body>
  <nav class="navbar bg-primary">
        <div class="container-fluid info-position" >
          <a class="navbar-brand" style="padding-top: 0px" href="/links">
            <img src="/img/navbar.png" width="60" height="60" class="d-inline-block align-top" alt="">
          </a>
          <ul class="nav navbar-nav navbar-left" style="padding-top: 15px" >
            <li>Bookmarks. Your way of remembering</li>
          </ul>

          <% if current_user %>
            <ul class="nav navbar-nav navbar-right" style="padding-top: 15px">
              Welcome, <%= current_user.email %>
            </ul>
            <ul class="nav navbar-nav navbar-right">
              <form method='post' class="navbar-brand bg-primary" action='/sessions'>
                <input type='hidden' name='_method' value='delete'>
                <input type='submit' class="btn btn-primary" value='Log out'
                style="border-width: 0px; padding: 0px">
              </form>
            </ul>

            <% else %>
              <ul class="nav navbar-nav navbar-right">
                <li>
                  <a class = "btn btn-primary" href="/users/new">Sign Up </a>
                </li>
                <li>
                  <a class = "btn btn-primary" href="/sessions/new">Sign In </a>
                </li>
              </ul>
            <% end %>
        </div>
      </nav>

      <% if flash[:errors] && !flash[:errors].empty? %>
      <div class="alert alert-success" role="alert">
        <h4 class="alert-heading">Please refer to the following errors below:</h4>
        <hr>
        <ul id='errors'>
          <% flash[:errors].each do |error| %>
            <li class="mb-0"><%= error %></li>
          <% end %>
        </ul>
      </div>
      <% end %>

    <% if flash[:notice] %>
      <div id='notice'>
        <%= flash[:notice] %>
      </div>
    <% end %>

    <%= yield %>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
</html>
