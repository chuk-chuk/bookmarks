<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link href="<%= url("/css/custom.css")%>" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Quicksand:300|Fredoka+One" rel="stylesheet">
    <title>Bookmarks</title>
  </head>

  <body>
    <nav class="navbar navbar-light bg-light">
      <div class=info-position>
      <span>
        <img src="/img/navbar.png" width="50" height="50" class="d-inline-block align-top" alt="">
        Bookmarks. Your way of remembering </span></div>

    <% if current_user %>
    <div class=welcome-message>
      <span>Welcome, <%= current_user.email %>
      <form method='post' action='/sessions'>
        <input type='hidden' name='_method' value='delete'>
        <input class="btn btn-primary" type='submit' value='Sign out'>
      </form></span></div>
    <% end %>

  </nav>

    <% if flash[:errors] && !flash[:errors].empty? %>
    <div class="alert alert-success" role="alert">
      <h4 class="alert-heading">Please refer to the following errors below:</h4>
      <hr>
      <ul id='errors'>
        <% flash[:errors].each do |error| %>
          <li class="mb-0"><%= error %></li>
        <% end %>
      </ul>
    </div>
    <% end %>

    <% if flash[:notice] %>
      <div id='notice'>
        <%= flash[:notice] %>
      </div>
    <% end %>

    <%= yield %>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
</html>
