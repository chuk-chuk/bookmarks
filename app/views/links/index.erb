<h1> Links </h1>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %><br/>
      URL: <%= link.url %><br/>
      Tags: <%= link.tags.map(&:name).join(', ') %>
    </li><br/>
  <% end %>
</ul>

<a href="links/new">Add new here</a>
